{
  "name": "windows-message-mcp",
  "version": "1.0.0",
  "description": "MCP server that receives messages POSTed from Windows 11 over HTTP and exposes them to Claude as tools",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "windows-message-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
